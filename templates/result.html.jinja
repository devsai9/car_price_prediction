<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Car Price Prediction</title>
    <style>
        #container {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
        }

        a {
            color: black;
        }
    </style>
</head>
<body>
    <a href="/">&lt; Home</a>
    <div id="container">
        <h1>Car Price Predictor</h1>
        <p>
            {% if prediction %}
                Your car is worth approximately ${{ prediction }}
            {% else %}
                There was an error
            {% endif %}
        </p>
    </div>
</body>
</html>